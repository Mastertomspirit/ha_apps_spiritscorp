name: "CI & App Validation"

on:
  push:
    branches: ["main" , "fix/*"]
  pull_request:
    branches: ["main"]

env:
  ARCHITECTURES: '["amd64", "aarch64"]'

jobs:
  resolve-targets:
    name: "Discover App Targets"
    runs-on: ubuntu-latest
    outputs:
      targets: ${{ steps.set-targets.outputs.targets }}
    steps:
      - name: "Checkout Repository"
        uses: actions/checkout@v6

      - name: "Find all app folders"
        id: set-targets
        run: |
          TARGETS_JSON=$(find . -maxdepth 2 -name "Dockerfile" -exec dirname {} \; | sed 's|^\./||' | jq -R . | jq -s -c .)
          echo "targets=${TARGETS_JSON}" >> $GITHUB_OUTPUT

  test_mimir:
    name: "Validate Mimir App & Dockerfile"
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout Repository"
        uses: actions/checkout@v7

      - name: "Set up Python & ShellCheck"
        uses: actions/setup-python@v7
        with:
          python-version: "3.14"

      - name: "Install yamllint & pyyaml"
        run: pip install pyyaml yamllint

      - name: "Run ShellCheck on test scripts"
        run: |
          find . -type f -name "*.sh" -exec shellcheck -x {} +

      - name: "Run App Test Suite"
        run: |
          chmod +x mimir/tests/validate.sh
          mimir/tests/validate.sh

  init:
    name: "Initialize Build Matrix for ${{ matrix.target }}"
    needs: [resolve-targets, test_mimir]
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.matrix.outputs.matrix }}
    steps:
      - name: Build matrix from architectures
        id: matrix
        shell: bash
        env:
          ARCHITECTURES: ${{ env.ARCHITECTURES }}
          REGISTRY_PREFIX: "ghcr.io/${{ github.repository_owner }}"
          TARGETS: ${{ needs.resolve-targets.outputs.targets }}
        run: |
          matrix='{"include":[]}'
          for target in $(jq -r '.[]' <<< "${TARGETS}"); do
            IMAGE_NAME="ha-spiritscorp-${target}"
            for arch in $(jq -r '.[]' <<< "${ARCHITECTURES}"); do
              case "${arch}" in
                amd64)   os="ubuntu-24.04" ;;
                aarch64) os="ubuntu-24.04-arm" ;;
                *)
                  echo "::error::Unsupported architecture: ${arch}. Supported values: amd64, aarch64"
                  exit 1
                  ;;
              esac
              matrix=$(jq -c \
                --arg target "$target" \
                --arg arch "$arch" \
                --arg os "$os" \
                --arg image "${REGISTRY_PREFIX}/${arch}-${IMAGE_NAME}" \
                '.include += [{target: $target, arch: $arch, os: $os, image: $image}]' <<< "${matrix}")
            done
          done
          echo "matrix=${matrix}" >> "$GITHUB_OUTPUT"
          jq . <<< "${matrix}"

  verify_build:
    name: "Verify ${{ matrix.arch }} Docker Build (${{ matrix.target }})"
    needs: [resolve-targets, init]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read # To check out the code
      id-token: write # Write needed for Cosign signing (issue OIDC token for signing)
      packages: write # To push built images to GitHub Container Registry
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.init.outputs.matrix) }}
    steps:
      - name: "Checkout Repository"
        uses: actions/checkout@v6

      - name: "Build image (dry-run)"
        uses: home-assistant/builder/actions/build-image@2026.09.0
        with:
          arch: ${{ matrix.arch }}
          context: ${{ matrix.target }}
          container-registry-password: ${{ secrets.GITHUB_TOKEN }}
          image: ${{ matrix.image }}
          push: "false"
          image-tags: |
            ${{ github.event.release.tag_name || inputs.version }}
          version: ${{ github.ref_name }}